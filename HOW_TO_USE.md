# วิธีใช้ชุดไฟล์นี้ใน Google Antigravity

## ขั้นตอนสำหรับนักศึกษา (ในห้องอบรม)

1. เปิด Antigravity IDE → สร้าง Workspace/Project ใหม่ ตั้งชื่อ `student-crud`
2. คัดลอกโฟลเดอร์ `.agents/` (มี 3 ไฟล์: agents.md, implementation_plan.md, task.md)
   ไปวางไว้ที่ root ของ workspace — Antigravity จะอ่านโฟลเดอร์นี้อัตโนมัติ
3. เปิด chat กับ Agent แล้วพิมพ์สั่งงาน เช่น:

   > อ่านไฟล์ .agents/agents.md, .agents/implementation_plan.md และ .agents/task.md
   > แล้วเริ่มทำงานตาม task.md ทีละ Phase จนครบ Phase 6

4. ปล่อยให้ Agent เข้าโหมด PLANNING → EXECUTION → VERIFICATION ตามลำดับ
   คอยกด Approve เมื่อ Agent ขอสิทธิ์รันคำสั่งหรือสร้างไฟล์
5. เมื่อ Agent ทำถึง Phase 6 (ทดสอบ) เสร็จ ให้รันโปรเจกต์บน localhost ตรวจว่าใช้งานได้จริง
6. **Phase 7 (Git) ให้นักศึกษาพิมพ์คำสั่งเองในเทอร์มินัลทุกบรรทัด** ไม่ให้ Agent รันแทน
   เพื่อฝึกความเข้าใจ Git โดยตรง (ตรงกับหัวข้อที่อบรมไปก่อนหน้า)

## ทำไมต้องแยกไฟล์แบบนี้
- `agents.md` = กติกา/ตัวตนของ AI (เหมือน "สัญญา" ว่า AI ต้องทำตามกฎอะไรบ้าง)
- `implementation_plan.md` = พิมพ์เขียวโครงสร้างโปรเจกต์ทั้งหมด
- `task.md` = รายการงานย่อยที่ตรวจสอบความคืบหน้าได้ (Agent ติ๊กเองระหว่างทำงาน)

การแยกแบบนี้ทำให้ AI ทำงานแม่นยำขึ้น ลดการ "เดาเอง" และนักศึกษาสามารถแก้ไข
ไฟล์ .md เหล่านี้เพื่อเปลี่ยนโจทย์โปรเจกต์ได้เอง (เช่น เปลี่ยนจาก "นักศึกษา" เป็น "สินค้า" หรือ "หนังสือ")
โดยไม่ต้องเขียนโค้ดเอง — เหมาะมากสำหรับสอนแนวคิด Spec-driven / Agentic Development
