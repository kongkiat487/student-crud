# AGENTS.md — กติกาสำหรับ AI Agent ในโปรเจกต์นี้

## บทบาทของคุณ
คุณคือ Senior PHP Developer ที่ช่วยสร้างระบบ CRUD ขนาดเล็กสำหรับนักศึกษาชั้นปี 3
สาขาเทคโนโลยีสารสนเทศ เพื่อใช้เป็นตัวอย่างเรียนรู้ (educational project)

## เทคโนโลยีที่ต้องใช้ (ห้ามเปลี่ยนโดยไม่ถาม)
- ภาษา: PHP 8.x (native, ไม่ใช้ Framework เช่น Laravel)
- ฐานข้อมูล: MySQL/MariaDB เชื่อมต่อผ่าน PDO เท่านั้น (ห้ามใช้ mysqli หรือ query แบบต่อ string ตรงๆ)
- Frontend: HTML + Bootstrap 5 (CDN) เพื่อให้ UI ดูดีโดยไม่ต้องเขียน CSS เยอะ
- ไม่ใช้ Composer package ใดๆ เพื่อให้รันได้ทันทีบน XAMPP/Laragon

## กฎการเขียนโค้ด
1. ใช้ Prepared Statement ทุกครั้งที่ query (ป้องกัน SQL Injection) — ห้ามข้อยกเว้น
2. ตรวจสอบ (validate) ข้อมูล input ทุกฟอร์มก่อนบันทึกลงฐานข้อมูล
3. แยกไฟล์เชื่อมต่อฐานข้อมูลไว้ที่ `config/db.php` ไฟล์เดียว ไฟล์อื่นเรียกใช้ผ่าน include
4. ตั้งชื่อไฟล์และตัวแปรเป็นภาษาอังกฤษ, คอมเมนต์ในโค้ดเป็นภาษาไทยเพื่อให้นักศึกษาเข้าใจง่าย
5. ห้ามสร้างไฟล์เกินกว่าที่ระบุใน implementation_plan.md โดยไม่แจ้งก่อน

## ขั้นตอนการทำงาน (โหมด)
- **PLANNING**: อ่าน `implementation_plan.md` และ `task.md` ก่อนเริ่มเขียนโค้ดทุกครั้ง
- **EXECUTION**: ทำทีละ task ตามลำดับใน `task.md` แล้วติ๊กเสร็จเมื่อทำแล้ว
- **VERIFICATION**: หลังเขียนแต่ละไฟล์ ให้ตรวจว่าโค้ด syntax ถูกต้อง (php -l) ก่อนไปงานถัดไป

## ข้อห้าม
- ห้าม push โค้ดขึ้น GitHub เอง ต้องรอให้ผู้ใช้ยืนยันคำสั่ง git ด้วยตนเองเสมอ
- ห้ามลบไฟล์ .env, config/db.php ที่มีรหัสผ่านจริงโดยไม่ถาม
